# On Success and Simplicity: A Second Look at Transferable Vision-Language Attack Pipeline (Under Review)

## Environment Setup

Install the dependencies according to the evaluation task you want to run.

| Task | Requirements File |
|---|---|
| Image–Text Retrieval | `requirements_re.txt` |
| Visual Entailment and Visual Grounding | `requirements_vevg.txt` |
| MLLM and API Evaluation | `requirements_mllm_api.txt` |

For example, to configure the environment for image–text retrieval:

```bash
pip install -r requirements_re.txt
```

## Run Attack and Evaluation

Run the corresponding script according to the source and target models.

For example, to transfer adversarial examples from **ALBEF** to **CLIP-CNN**:

```bash
python t_main_a2cc.py
```

## Bug Fix

In the main implementations of **SGA**, **DRA**, and **SA-AET**, the initial value of `index` in the `_tokenize()` function should be changed from `0` to `1`:

```python
def _tokenize(self, text):
    words = text.split(' ')

    sub_words = []
    keys = []

    # index = 0
    index = 1 

    for word in words:
        sub = self.tokenizer.tokenize(word)
        sub_words += sub
        keys.append([index, index + len(sub)])
        index += len(sub)

    return words, sub_words, keys
```

### Why Should `index` Start from 1?

The BERT tokenizer adds special tokens to the input sequence by default. For a single input sentence, the tokenized sequence has the following structure:

```text
[CLS] token_1 token_2 ... token_n [SEP]
```

Therefore, position `0` is occupied by the `[CLS]` token, and the first token of the original text starts at position `1`.

In contrast, `self.tokenizer.tokenize(word)` returns only the WordPiece tokens of the given word and does not include `[CLS]` or `[SEP]`. The `keys` generated by `_tokenize()` are later used to index the masked language model predictions, whose sequence positions correspond to the complete BERT input containing these special tokens.

Consequently, an offset of `1` is required to align each word and its WordPiece tokens with the corresponding positions in the BERT output.

If `index` starts from `0`, all word positions are shifted one position to the left. For example, the first word may incorrectly include the prediction at the `[CLS]` position while omitting its last WordPiece token. This positional misalignment causes incorrect substitute-token extraction and degrades the quality of the generated adversarial text.

### Results After the Fix

After correcting the token-position offset, SGA, DRA, and SA-AET achieve improvements on both text retrieval and image retrieval tasks.

<table>
  <thead>
    <tr>
      <th rowspan="3">Attack</th>
      <th colspan="4">CLIP<sub>ViT</sub></th>
    </tr>
    <tr>
      <th colspan="2">TR</th>
      <th colspan="2">IR</th>
    </tr>
    <tr>
      <th>R@1</th>
      <th>R@10</th>
      <th>R@1</th>
      <th>R@10</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>SGA</td>
      <td>32.39</td>
      <td>8.64</td>
      <td>44.07</td>
      <td>21.00</td>
    </tr>
    <tr>
      <td><strong>SGA, index = 1</strong></td>
      <td><strong>38.77</strong></td>
      <td><strong>11.99</strong></td>
      <td><strong>53.58</strong></td>
      <td><strong>26.91</strong></td>
    </tr>
    <tr>
      <td>DRA</td>
      <td>36.69</td>
      <td>8.84</td>
      <td>47.97</td>
      <td>22.76</td>
    </tr>
    <tr>
      <td><strong>DRA, index = 1</strong></td>
      <td><strong>40.98</strong></td>
      <td><strong>13.82</strong></td>
      <td><strong>56.60</strong></td>
      <td><strong>29.39</strong></td>
    </tr>
    <tr>
      <td>SA-AET</td>
      <td>37.81</td>
      <td>10.96</td>
      <td>50.48</td>
      <td>25.51</td>
    </tr>
    <tr>
      <td><strong>SA-AET, index = 1</strong></td>
      <td><strong>41.60</strong></td>
      <td><strong>14.74</strong></td>
      <td><strong>57.93</strong></td>
      <td><strong>30.38</strong></td>
    </tr>
    <tr>
      <td>SimVLA</td>
      <td>52.52</td>
      <td>17.58</td>
      <td>60.24</td>
      <td>33.32</td>
    </tr>
  </tbody>
</table>
